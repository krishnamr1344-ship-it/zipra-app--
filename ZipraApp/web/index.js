import { AppRegistry } from 'react-native';
import App from '../App';

AppRegistry.registerComponent('ZipraApp', () => App);
AppRegistry.runApplication('ZipraApp', { rootTag: document.getElementById('root') });
